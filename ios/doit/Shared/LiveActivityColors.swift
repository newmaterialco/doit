import SwiftUI

/// Fixed light palette for the lock-screen Live Activity (matches chowder-iOS coloring).
enum LiveActivityColors {
    static let canvas = Color.white
    static let card = Color.white
    static let primaryNavy = Color(red: 47 / 255, green: 59 / 255, blue: 84 / 255)
    static let cardText = Color.black
    static let iconForeground = Color.black.opacity(0.5)
    static let iconCircle = Color.black.opacity(0.12)
}
